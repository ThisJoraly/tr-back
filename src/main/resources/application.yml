spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/transport
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  application:
    name: transport
  jpa:
    hibernate:
      ddl-auto: update
springdoc:
  api-docs:
    path: /api-docs